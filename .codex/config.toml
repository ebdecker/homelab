# Repository-local Codex defaults converted conservatively from
# .claude/settings.local.json. This file is loaded only when the project is
# trusted by Codex.

approval_policy = "on-request"
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true
